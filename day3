ORM框架
Object Relational Mapping
	|
	|
对象－关系　映射　把关系数据库的结构映射到实例对象上。

在python中表示数据库，需要三层表示

第一先用元类，表示出数据库的整体框架：
在元类中描述出一个完整的表结构，例如字段。属性。还有操作表的sql的语句

第二就是父类。
把所有子类需要用到的对表的操作都集成到这个类里面，然后继承给子类，避免写重复的代码

第三就是子类。
因为每个表都不同，所以在这我们要根据数据库中的表创建子类

	
